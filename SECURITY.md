# 执行与隐私边界

LinSail 是以当前用户身份执行命令的工具，不是隔离环境或权限管理系统。

## 已有措施

- 所有模型发起的命令都需要用户逐条确认，默认拒绝；拒绝后本轮停止。
- 命令原文在执行前展示，拒绝不可见控制字符。常见危险命令有提示，但不使用关键词黑名单冒充沙箱。
- CLI 拒绝 root 启动。sudo 密码通过本机 PTY 输入，不经过模型聊天输入框。
- 配置不保存 API Key；仅记录凭证环境变量名称。配置写入使用用户私有临时文件与原子替换。
- 已配置凭证和常见 `*_API_KEY` / `*_API_TOKEN` 变量不传入子 Bash。
- 请求禁止 HTTP 重定向，避免 Bearer 凭证转发给意外地址；远程接口只接受 HTTPS。
- 出站请求中的当前模型密钥做精确值脱敏。HTTP 错误不回显服务器响应体。
- 不提供遥测；不自动保存聊天、命令输出或受管 Bash 历史。配置会持久化。
- 临时命令脚本保存在私有临时目录，命令结束清除。进程崩溃可能留下临时文件，应通过系统正常临时文件清理策略清理。

## 不能保证什么

同一用户进程仍可能读取其有权限访问的文件、进程信息和其他凭证。移除环境变量不是操作系统沙箱。sudo 后的命令具有更高权限。获得审批的命令也可能访问网络、修改系统或启动子进程。

工具输出可能包含敏感信息或提示注入；系统提示不保证模型永远忽略恶意文本。用户必须检查实际命令，而不是只看 AI 的解释。精确密钥替换不能检测所有形式的密钥、令牌和隐私信息。

默认自动执行输出会发往所选择的服务。手动终端内容不会自动上传，但其文件/系统修改仍可被后续审批命令检查并上传。模型服务自己的日志、隐私政策和计费由该服务负责。

Shell 的状态管道用于可靠识别完成与退出码，**不是防恶意进程伪造的安全协议**。不要让不可信代码获得执行审批。真实软件安装和跨发行版行为仍需集成验证。

## 报告问题

普通问题可以通过仓库 Issues 提交，删除 API Key、真实服务器地址、隐私输出和其他凭证。涉及凭证泄漏或可被利用的漏洞，请不要在公开 Issue 中贴出可用密钥；由仓库维护者启用 GitHub 私有漏洞报告后通过该入口提交细节。
