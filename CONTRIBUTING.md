# 参与 LinSail

欢迎提交问题与改进。问题报告请包含 Linux 发行版、Python/Bash 版本、复现步骤，以及删除密钥与个人信息后的错误信息。

运行 `PYTHONPATH=src python3 -m unittest discover -s tests -v` 验证。涉及终端代码的修改必须在真实 Linux 上运行 PTY 测试。保持标准库优先，不引入默许所有命令的执行模式，不将托管服务主密钥写入客户端。

修改保持集中：描述用户遇到的问题、实际行为变化和验证结果。涉及付费服务的变更需要真实服务端接口，不能用假余额或演示支付冒充可售卖产品。
